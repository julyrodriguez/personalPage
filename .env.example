# Secret Key para la autenticación de Endpoints de Ingesta (Webhooks / Agentes externos)
# Puede ser enviado como Header "Authorization: Bearer <API_SECRET_KEY>" o "x-api-key: <API_SECRET_KEY>"
API_SECRET_KEY=pos_hub_secret_token_2025_power_user

# Configuración del Clima (Open-Meteo)
NEXT_PUBLIC_DEFAULT_CITY="Buenos Aires, AR"
NEXT_PUBLIC_DEFAULT_LAT="-34.6037"
NEXT_PUBLIC_DEFAULT_LON="-58.3816"

# Persistencia de Datos
# Directorio donde se almacenan JSON/SQLite para persistencia local ligera
DATA_DIR="./data"
